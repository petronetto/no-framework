default:
  autoload:
    '': "%paths.base%/tests/features/bootstrap"
  suites:
    default:
      contexts:
        - FeatureContext:
            username: nofw
            password: supersecret
      paths:
        - "%paths.base%/tests/features"